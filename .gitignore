# Wiktionary contribution tooling.
# Credentials, and the scratch space a run works in. The records/ directory
# beside them is deliberately NOT ignored - it is the audit trail of every edit
# the tool has made.
tools/wiktionary/.credentials.json
tools/wiktionary/work/

.DS_Store
